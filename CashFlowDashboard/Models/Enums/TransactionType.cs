namespace CashFlowDashboard.Models.Enums;

// Defines the direction of cash flow for a transaction.
public enum TransactionType
{
    // Cash inflow (e.g., revenue, payments received).
    Income = 0,

    // Cash outflow (e.g., expenses, purchases, payments made).
    Expense = 1
}
